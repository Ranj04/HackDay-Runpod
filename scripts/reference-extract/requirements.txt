# Reference-extraction pipeline deps. Python 3.9+.
# opencv-python bundles its own video decoder, so no system ffmpeg is required.
mediapipe>=0.10.35
opencv-python>=4.9
numpy>=1.24
# Optional, only for downloading source clips (see README):
# yt-dlp>=2024.1
