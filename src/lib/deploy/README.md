# Deployment status

- Production frontend: <https://echo-form-coach.vercel.app>
- Host: Vercel (`echo-form-coach`)
- Verified routes: `/`, `/capture`, `/results`, `/history`
- Supabase: local-demo fallback until project credentials are configured
- Nebius reference-builder: not run; no Nebius credentials were available

The demo therefore continues to use the curated reference. No
`fixtures/reference/generated/` output exists, and no GPU-generated reference
should be claimed.
